<%= form_with(model: average_region, local: true) do |form| %>
  <% if average_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(average_region.errors.count, "error") %> prohibited this average_region from being saved:</h2>

      <ul>
      <% average_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :region %>
    <%= form.text_field :region %>
  </div>

  <div class="field">
    <%= form.label :average %>
    <%= form.text_field :average %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
